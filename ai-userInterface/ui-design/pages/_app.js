// This file is for Next.js Pages Router compatibility
// The application uses App Router (app directory), but this file is included per requirements

import '@/styles/globals.css';

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
