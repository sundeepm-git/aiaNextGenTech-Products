# Requirements for Azure-to-Terraform Refactor Engine
# Standard library (built-in, no install needed):
# pathlib, re, shutil, datetime, subprocess, sys, os, tempfile, time, argparse

# External dependencies
PyYAML>=6.0
python-dotenv>=1.0.0
requests>=2.32.0  # For GitHub API integration
python-hcl2>=7.0.0  # For Terraform HCL file parsing
